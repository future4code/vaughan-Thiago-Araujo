export const goToMega = (Navigate) => {
  Navigate("/");
};

export const goToQuina = (Navigate) => {
  Navigate("/quina");
};

export const goToLotoFa = (Navigate) => {
  Navigate("/lotofacil");
};

export const goToLotoMa = (Navigate) => {
  Navigate("/lotomania");
};

export const goToTime = (Navigate) => {
  Navigate("/timemania");
};

export const goToDia = (Navigate) => {
  Navigate("/dia-de-sorte");
};
